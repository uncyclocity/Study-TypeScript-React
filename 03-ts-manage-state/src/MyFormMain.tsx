import MyForm from "./MyForm";

export default function MyFormMain() {
  const onSubmit = (form: { name: string; description: string }) => {
    console.log(form);
  };
  return <MyForm onSubmit={onSubmit} />;
}
